chezmoi.toml

# Ignore Windows-specific paths unless on Windows
{{ if ne .chezmoi.os "windows" }}
AppData/Local/nvim/*
{{ end }}

# Ignore Linux-specific paths unless on normal Linux
{{ if or (ne .chezmoi.os "linux") (.chezmoi.kernel.osrelease | lower | contains "microsoft") }}
.config/nvim/*
{{ end }}
