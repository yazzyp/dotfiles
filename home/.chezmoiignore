chezmoi.toml

# Ignore Windows-specific paths unless on Windows
{{ if ne .chezmoi.os "windows" }}
AppData/Local/nvim/*
{{ end }}

# Ignore Linux-specific paths unless on Linux or WSL
{{ if ne .chezmoi.os "linux" }}
  {{ if not (.chezmoi.kernel.osrelease | lower | contains "microsoft") }}
    .config/nvim/*
  {{ end }}
{{ end }}
